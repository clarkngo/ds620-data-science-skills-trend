# ds620

# Notes for running locally

```
# create and activate your virtual environment first
python -m venv venv
source venv/bin/activate    # (on macOS/Linux)
venv\Scripts\activate       # (on Windows)

# then install all dependencies
pip install -r requirements.txt
```

> Note: if you install new pip packages please run `pip freeze > requirements.txt`

# Download and install data

Data Source : https://www.kaggle.com/datasets/asaniczka/data-science-job-postings-and-skills/data?select=job_postings.csv

1. Download Data Source
2. Unzip and move to directory (Keep default file name, archive and file structure)
